@* @model IEnumerable<Your_Ride.ViewModels.BookViewModel.BookVM>

@{
    ViewData["Title"] = "User's Bookings";
}

<h2 class="text-center my-4">Your Ride Bookings</h2>

@if (Model != null && Model.Any())
{
    <table class="table table-striped table-hover shadow-sm">
        <thead class="table-dark">
            <tr>
                <th>#</th>
                <th>User</th>
                <th>Appointment</th>
                <th>Max Amount</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var book in Model)
            {
                <tr>
                    <td>@book.Id</td>
                    <td>@(book.User != null ? $"{book.User.FirstName} {book.User.LastName}" : "N/A")</td>
                    <td>@(book.Appointment != null ? book.Appointment.Date.ToString("yyyy-MM-dd") : "No Appointment")</td>
                    <td>$@book.MaxAmount.ToString("F2")</td>
                    <td>
                        <a asp-action="GetBookByID" asp-route-id="@book.Id" class="btn btn-info btn-sm">View</a>
                        <a asp-action="EditBook" asp-route-id="@book.Id" class="btn btn-warning btn-sm">Edit</a>
                        <a asp-action="DeleteBook" asp-route-id="@book.Id" class="btn btn-danger btn-sm">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="alert alert-warning text-center">
        <h4>No bookings found.</h4>
        <p>Start by <a asp-action="CreateBook" class="btn btn-success">Creating a New Booking</a>.</p>
    </div>
}

<div class="text-center mt-4">
    <a asp-action="GetAllBooks" class="btn btn-secondary">Back to All Books</a>
</div>
 *@